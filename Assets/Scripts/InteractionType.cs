public enum InteractionType
{
    None,           // Não interativo
    Default,        // Interação padrão (ex: porta fechada, botão não pressionado)
    Alternative     // Estado alternativo (ex: porta aberta, botão pressionado)
}
